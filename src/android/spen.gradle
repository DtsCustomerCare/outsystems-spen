repositories{    
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['pen-v4.1.2_full.aar'])
}

android {
    defaultConfig  {
        ndk {
            abiFilters "armeabi"
        }
    }
}