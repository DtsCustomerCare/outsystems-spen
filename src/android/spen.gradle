repositories{    
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    compile(name: 'pen-v4.1.2_full', ext: 'aar')
}

android {
    defaultConfig  {
        ndk {
            abiFilters "armeabi"
        }
    }
}